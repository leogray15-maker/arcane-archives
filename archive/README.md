# Archived pages

Kept for reference only. This folder is excluded from the deployed site
(`scripts/build-site.mjs`), and the old URLs redirect to `/watchtower/`
(`vercel.json`).

- `world-map.html` + `world-geo.js` — Watchtower v1 (canvas globe)
- `watchtower.html` — earlier Leaflet-based Watchtower (unlinked)
- `global-intelligence.html` — earlier intelligence page (unlinked)
